[pytest]
pythonpath = .
testpaths = tests
markers =
    skip_hermetic_audit: opt out of the autouse hermetic_semantic_audit fixture to test the real audit_batch_semantic_integrity implementation via _dispatch_llm_completion mocks
